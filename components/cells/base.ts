import { CELL_BASE_SIZE } from "../../config/index.ts";

export const BASE_CLASS_NAMES = `flex items-center justify-center h-[${CELL_BASE_SIZE}px] w-[${CELL_BASE_SIZE}px] focus:outline-none rounded`;
